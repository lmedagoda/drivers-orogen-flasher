# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(flasherTaskLib)
ADD_LIBRARY(${FLASHER_TASKLIB_NAME} SHARED 
    ${FLASHER_TASKLIB_SOURCES})
add_dependencies(${FLASHER_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${FLASHER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${FLASHER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${FLASHER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${FLASHER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${FLASHER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${FLASHER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${FLASHER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${FLASHER_TASKLIB_HEADERS}
    DESTINATION include/orocos/flasher)

